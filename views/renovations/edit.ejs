<!DOCTYPE html>
<html>
<head>
  <title>Edit Renovation</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container mt-4">
  <h2>Edit Renovation</h2>
  <form method="POST" action="/admin/renovations/edit/<%= renovation.id %>" enctype="multipart/form-data">
    <div class="mb-3">
      <label>Service Name</label>
      <input name="service_name" class="form-control" value="<%= renovation.service_name %>" required>
    </div>

    <div class="mb-3">
      <label>Description</label>
      <textarea name="description" class="form-control" rows="4" required><%= renovation.description %></textarea>
    </div>

    <% if (renovation.image_url) { %>
      <div class="mb-3">
        <img src="<%= renovation.image_url %>" alt="Current Image" style="max-width: 200px;">
        <input type="hidden" name="existing_image" value="<%= renovation.image_url %>">
      </div>
    <% } %>

    <div class="mb-3">
      <label>New Image (optional)</label>
      <input type="file" name="image" class="form-control" accept="image/*">
    </div>

    <button type="submit" class="btn btn-primary">Update</button>
  </form>
</div>
</body>
</html>